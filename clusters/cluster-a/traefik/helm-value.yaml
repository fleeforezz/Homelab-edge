deployment:
  replicas: 2

service:
  type: LoadBalancer

ports:
  web:
    expose: true
  websecure:
    expose: true

additionalArguments:
  - "--providers.kubernetescrd"
  - "--providers.kubernetesingress"
  - "--ping"
